# Complexity Kink Research ,  direct dependencies
# Requires Python 3.12+
#
# Versions pinned to those used in the study. Transitive dependencies are
# resolved by pip against these pins; create an isolated venv before
# installing to avoid mixing with a system or conda environment.

# Numerics & data
numpy==1.26.4
pandas==2.2.3
scipy==1.15.2
pyarrow==23.0.0

# Modeling (IV/2SLS core + survivorship-robustness RF)
scikit-learn==1.6.1
linearmodels==7.0
statsmodels==0.14.6
joblib==1.4.2

# Static-analysis complexity (Stage A baseline CC, multi-language)
lizard==1.21.0
tree-sitter==0.25.2
tree-sitter-python==0.25.0

# Visualization (publication + interactive)
matplotlib==3.10.1
seaborn==0.13.2
plotly==6.5.0
kaleido==1.2.0

# Dataset ingestion (NVIDIA OpenCodeInstruct from Hugging Face)
huggingface_hub==0.34.4
requests==2.32.5

# Model APIs (cross-model generation pipeline ,  API-only, portable)
openai==1.99.9
anthropic==0.89.0
google-generativeai==0.7.2
google-genai==1.73.1
